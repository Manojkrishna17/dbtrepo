version: 2

models:
  - name: stg_department
    columns:
      - name: department_id
        tests:
          - unique
          - not_null

      - name: department_name
        tests:
          - not_null
          # - dbt_utils.expression_is_true:
          #     expression: "length(department_name) > 2"   # no very short/empty names

      # - name: created_date
      #   tests:
      #     - dbt_utils.expression_is_true:
      #         expression: "created_date <= current_date()"